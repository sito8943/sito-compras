export enum Tables {
  Checklists = 'checklists',
  Products = 'products',
}

export enum TablesCamelCase {
  Checklists = 'checklists',
  Products = 'products',
}

export enum EntityName {
  Checklist = 'checklist',
  Product = 'product',
}

